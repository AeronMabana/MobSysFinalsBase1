@page "/login"

<!-- Custom Modern Login UI/UX (Not based on Bootstrap defaults) -->
<style>
    .custom-login-bg {
        min-height: 100vh;
        background: linear-gradient(135deg, #f3f3f3 0%, #c8e6ff 100%);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .custom-login-card {
        background: #fff;
        border-radius: 20px;
        box-shadow: 0 8px 32px rgba(60, 80, 140, 0.11);
        padding: 2.5rem 2rem 2rem 2rem;
        width: 100%;
        max-width: 370px;
        transition: box-shadow 0.3s;
        position: relative;
    }

        .custom-login-card:hover {
            box-shadow: 0 16px 48px 4px rgba(60, 80, 140, 0.18);
        }

    .custom-login-title {
        font-size: 2rem;
        font-weight: 700;
        color: #23508d;
        margin-bottom: 1.5rem;
        letter-spacing: 1px;
        text-align: center;
    }

    .custom-input {
        width: 100%;
        padding: 0.85rem 1rem;
        border: none;
        border-radius: 12px;
        background: #f6fafd;
        margin-bottom: 1.5rem;
        font-size: 1.08rem;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 6px rgba(60, 80, 140, 0.03);
    }

        .custom-input:focus {
            background: #e3f1ff;
            outline: none;
            box-shadow: 0 2px 12px rgba(35,80,141, 0.07);
        }

    .custom-label {
        display: block;
        margin-bottom: 0.5rem;
        color: #23508d;
        font-weight: 500;
        font-size: 1rem;
        letter-spacing: 0.5px;
    }

    .custom-login-btn {
        width: 100%;
        padding: 0.9rem;
        background: linear-gradient(90deg, #4f8cff 60%, #23508d 100%);
        border: none;
        border-radius: 12px;
        color: #fff;
        font-size: 1.15rem;
        font-weight: 600;
        letter-spacing: 1px;
        cursor: pointer;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 18px rgba(79, 140, 255, 0.11);
        margin-top: 0.5rem;
    }

        .custom-login-btn:hover, .custom-login-btn:focus {
            background: linear-gradient(90deg, #23508d 60%, #4f8cff 100%);
            box-shadow: 0 8px 32px rgba(79, 140, 255, 0.18);
        }

    .custom-alert {
        background: #ffe6e6;
        color: #b71c1c;
        border-radius: 8px;
        padding: 0.75rem 1rem;
        margin-bottom: 1.2rem;
        text-align: center;
        font-size: 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.6em;
    }

        .custom-alert i {
            font-size: 1.1rem;
        }
</style>

<div class="custom-login-bg">
    <div class="custom-login-card">
        <div class="custom-login-title">Welcome Back</div>
        @if (!string.IsNullOrWhiteSpace(StatusMessage))
        {
            <div class="custom-alert">
                <i class="fa fa-info-circle"></i>
                @StatusMessage
            </div>
        }
        <form @onsubmit="LoginUser">
            <label for="username" class="custom-label">Username</label>
            <input id="username" class="custom-input" type="text" @bind="Model.Username" placeholder="Enter your username" autocomplete="username" />

            <label for="password" class="custom-label">Password</label>
            <input id="password" class="custom-input" type="password" @bind="Model.Password" placeholder="Enter your password" autocomplete="current-password" />

            <button type="submit" class="custom-login-btn">
                <i class="fas fa-sign-in-alt"></i>&nbsp;Log In
            </button>
        </form>
    </div>
</div>